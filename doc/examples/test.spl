(*(fn f => fn x => if (f (f x)) then 1 else 0) (fn b => not b) true*)
fn x => fn y =>
            let a = ref x in
              let b = ref y in
                let c = ref 0 in
                  (while !b <> 0 do c := !a; a := !b; b := !c % !b);
                  !a
                end
              end
            end

(* ==> 1029 *)